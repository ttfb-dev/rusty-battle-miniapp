import React from 'react'

import { Panel } from 'components/Panel'

export const AssemblyPanel = ({ id }) => {
  return (
    <Panel id={id} header="AssemblyPanel">
      <h1>Assembly</h1>
    </Panel>
  )
}
